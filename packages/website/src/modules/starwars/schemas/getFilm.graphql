#import './baseFilm.graphql'

query FILM_QUERY($id: Int) {
  getFilm(id: $id) {
    ...BaseFilm
    opening_crawl
    director
    producer
    release_date
    species {
      name
    }
    starships {
      name
    }
    vehicles {
      name
    }
    characters {
      name
    }
    planets {
      name
    }
  }
}
