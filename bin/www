#!/usr/bin/env node

var app = require('../app');
var lex = require('letsencrypt-express');

lex.create({
  server: 'staging',
  email: 'mattailes@gmail.com',
  agreeTos: true,
  approveDomains: [ 'mattailes.net' ],
  app: require('express')().use('/', function (req, res) {
    res.end('Hello, World!');
  })
}).listen(3000, 5001);
